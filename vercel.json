{
  "version": 2,
  "builds": [
    { 
      "src": "api/*.js", 
      "use": "@vercel/node" 
    }
    // Hapus build statis jika Vercel dapat mendeteksinya secara otomatis
  ]
  // Hapus semua routes
}